{
  "name": "travel-rpg",
  "version": "0.1.0",
  "description": "Gamified scrollytelling presentation — Atlys travel experience",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:all": "vitest run && playwright test"
  },
  "dependencies": {
    "phaser": "^3.85.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.4.0",
    "@playwright/test": "^1.45.0",
    "vitest": "^1.6.0"
  }
}
